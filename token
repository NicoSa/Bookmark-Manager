token
- zeitliche abhängigkeit

	- creation timestamp mit/im token -> eine Stunde gültig 1h, dann wird er gelöscht
		token : { erstellung : gültigkeitsZeitspanne }



class Token

   @exiry  = @created + @validity_period

   @value 

   def initialize(expire_after)
      
      @expiry = @created + expire_after
      @value = (1..64).map{('A'..'Z').to_a.sample}.join
   end


   def redeem():
      if Time.now >= expiry
         bitch around!!!
      else:
          alles geil()
    
      delete()
   end
end 


Entity: 
 - Identity
 - Continuity 
 - Can have the same value more than Once

Person(Nico, London) != Person(Nico, London)


Value Object (Struct):
 - Value
 - Identity is irrelevant

Blue Crayon == Blue Crayon
5 == 5 


Token (abcdef)
 ==
Token (abcdef)